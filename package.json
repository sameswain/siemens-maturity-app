{
  "name": "siemens-maturity-app",
  "version": "1.0.0",
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && npm install && node src/index.js"
  }
}
